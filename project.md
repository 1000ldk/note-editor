# iOSアプリ (SwiftUI) 開発プロジェクト計画

## フェーズ1: バックエンドAPIのモバイル対応 (Next.js側)
- [ ] **モバイル用認証エンドポイントの作成**
  - CookieベースのNextAuthと併用できるJWT形式等のトークン発行API (pp/api/auth/mobile-login/route.ts など) の実装。
- [ ] **各APIルートの認証ロジック拡張**
  - Web(Session)とモバイル(トークン)の両方でユーザーを特定しリクエストを処理できるよう、既存のAPI (memos, 	opics) の認証処理 (lib/auth.ts等) を共通化・改修。
- [ ] **モバイル用APIの動作確認**
  - PostmanやcURLで、トークンを用いたAPIの送受信が正しく行えるか検証。
  - Playwright等で既存のWeb側機能にデグレが起きていないか確認。

## フェーズ2: iOSプロジェクトのセットアップ (SwiftUI)
- [ ] **Xcodeプロジェクトの初期構築**
  - プロジェクトルートに ios/ ディレクトリを切り、新規SwiftUIプロジェクトを作成。
- [ ] **データモデル (エンティティ) の定義**
  - 既存のPrismaスキーマ (schema.prisma の Memo, Topic 等) に対応する、Codable を適用したSwift構造体を作成。
- [ ] **API通信・ネットワーク層の構築**
  - ログイン処理、セッショントークンの安全な保存 (Keychainの利用)、および各種リクエストへの Authorization: Bearer <token> 自動付与処理の実装。

## フェーズ3: SwiftUIによるUI・機能の実装
- [ ] **認証フローの実装**
  - ログイン、サインアップ画面の構築。トークンの有無に応じた画面のルーティング設定。
- [ ] **ダッシュボード・一覧系画面の実装**
  - トピック別・カテゴリ別のメモ一覧画面の実装。
- [ ] **編集・作成画面の実装**
  - 新規メモの作成画面、詳細・編集UIの実装。
- [ ] **キャンバス (React Flow) 部分の代替対応**
  - ネイティブのSwiftUI(Canvas, Path, Gesture等)で再実装するか、複雑さと工数を考慮してWKWebViewで既存のWebアプリのキャンバスコンポーネントのみをオーバーレイ表示するかの方針決定および実装。

## フェーズ4: 仕上げ・テスト
- [ ] **全体結合テスト**
  - シミュレーター、および実機(iPhone/iPad)での動作確認。
- [ ] **デザイン微調整**
  - iOS標準のヒューマンインターフェイスガイドライン (HIG) に沿ったUI/UXのブラッシュアップ。
